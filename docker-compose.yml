name: minio

services:
  rag:
    build:
      context: .
      dockerfile: Dockerfile.analytics
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      ANTHROPIC_API_KEY: "${ANTHROPIC_API_KEY}"
      DUCKDB_PATH: "/data/warehouse.duckdb"
      EMBEDDINGS_PATH: "/data/embeddings.duckdb"
      REDEN_PATH: "/data/reden.duckdb"
      REDEN_EMBEDDINGS_PATH: "/data/reden_embeddings.duckdb"
    volumes:
      - "${PWD}/warehouse.duckdb:/data/warehouse.duckdb:ro"
      - "${PWD}/embeddings.duckdb:/data/embeddings.duckdb:ro"
      - "${PWD}/reden.duckdb:/data/reden.duckdb:ro"
      - "${PWD}/reden_embeddings.duckdb:/data/reden_embeddings.duckdb:ro"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  mcp:
    build:
      context: .
      dockerfile: Dockerfile.analytics
    command: ["python", "-m", "analytics.mcp_server"]
    ports:
      - "127.0.0.1:8765:8765"
    environment:
      DUCKDB_PATH: "/data/warehouse.duckdb"
      REDEN_PATH: "/data/reden.duckdb"
      REDEN_EMBEDDINGS_PATH: "/data/reden_embeddings.duckdb"
      EMBEDDINGS_PATH: "/data/embeddings.duckdb"
      MCP_HOST: "0.0.0.0"
      MCP_PORT: "8765"
    volumes:
      - "${PWD}/warehouse.duckdb:/data/warehouse.duckdb:ro"
      - "${PWD}/reden.duckdb:/data/reden.duckdb:ro"
      - "${PWD}/reden_embeddings.duckdb:/data/reden_embeddings.duckdb:ro"
      - "${PWD}/embeddings.duckdb:/data/embeddings.duckdb:ro"
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD}"
    ports:
      - "127.0.0.1:9000:9000"
      - "127.0.0.1:9001:9001"
    volumes:
      - /srv/minio/data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 30s
      timeout: 20s
      retries: 3
